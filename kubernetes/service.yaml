apiVersion: v1
kind: Service
metadata:
  name: squash-bokeh
  labels:
    app: squash
spec:
  selector:
    app: squash
    tier: bokeh
  ports:
    - protocol: TCP
      port: 443
      targetPort: 443
  type: LoadBalancer
